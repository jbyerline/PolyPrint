import React from "react";
import Typography from "@mui/material/Typography";

export default function ThemeSettings(props) {
  const { open, label, icon } = props;

  return <Typography>Hello World 2</Typography>;
}
