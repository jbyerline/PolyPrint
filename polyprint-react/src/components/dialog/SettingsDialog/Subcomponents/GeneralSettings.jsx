import React from "react";
import Typography from "@mui/material/Typography";

export default function GeneralSettings(props) {
  const { open, label, icon } = props;

  return <Typography>Hello World 1</Typography>;
}
