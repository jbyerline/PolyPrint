version: "2"
services:
  polyprint:
    build:
      dockerfile: Dockerfile
    ports:
      - "80:80"
      - "5000:5000"
    environment:
      NODE_ENV: production
    image: "registry.byerline.me/polyprint:${APP_VERSION}"